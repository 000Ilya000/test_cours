//! ХУКИ

describe('getLength', () => {

    describe('', () => {
        // // Выполняется один раз перед выполнением всех тестов в данной группе(describe)
        // beforeAll(() => {
        //     console.log('basic setup');
        // })
    
        // // Выполняется перед каждым тестом в данной группе
        // beforeEach(() => {
        //     console.log('run setup before each test case'); 
        // })
    
        // Выполняется после каждого теста в данной группе
        // afterEach(() => {
        //     console.log('teardown after each test case');
        // })
    
        // // Выполняется один раз после выполнения всех тестов в данной группе
        // afterAll(() => {
        //     console.log('clean up'); 
        // })
    })

    // Задаем тест, который еще не реализован (TODO)
    it.todo('sth TODO') // Здесь можно добавить описание того, что нужно сделать в будущем
})
